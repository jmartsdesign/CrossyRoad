
class Log extends Car {

  Log(float x, float y, float w, float h, float s) {
    super (x, y, w, h, s );  // super -Palavra-chave usada para referenciar a superclasse de uma subclasse.
  }
}
